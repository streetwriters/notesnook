
  it('app loaded', async () => {
    await expect(element(by.id('mobile_main_view'))).toBeVisible();
  })

  it('open_editor', async () => {
    await expect(element(by.id('mobile_main_view'))).toBeVisible();
  })


