diff --git a/node_modules/@react-navigation/drawer/src/views/Drawer.tsx b/node_modules/@react-navigation/drawer/src/views/Drawer.tsx
index 6a70f2f..8cf1397 100644
--- a/node_modules/@react-navigation/drawer/src/views/Drawer.tsx
+++ b/node_modules/@react-navigation/drawer/src/views/Drawer.tsx
@@ -81,6 +81,7 @@ type Props = {
   open: boolean;
   onOpen: () => void;
   onClose: () => void;
+  onStateChange?:(state:any) => void
   gestureEnabled: boolean;
   swipeEnabled: boolean;
   drawerPosition: 'left' | 'right';
@@ -114,6 +115,7 @@ export default class DrawerView extends React.Component<Props> {
     keyboardDismissMode: 'on-drag',
     hideStatusBar: false,
     statusBarAnimation: 'slide',
+    onStateChange:() => {}
   };
 
   componentDidUpdate(prevProps: Props) {
@@ -488,10 +490,18 @@ export default class DrawerView extends React.Component<Props> {
     },
   ]);
 
+
   private handleGestureStateChange = event([
     {
       nativeEvent: {
-        state: (s: Animated.Value<number>) => set(this.gestureState, s),
+        state: (s: Animated.Value<number>) => block([
+          set(this.gestureState, s),
+          call([this.gestureState],(state) => {
+              if (this.props.onStateChange) {
+                  this.props.onStateChange(state);
+              }
+          })
+          ]),
       },
     },
   ]);
diff --git a/node_modules/@react-navigation/drawer/src/views/DrawerView.tsx b/node_modules/@react-navigation/drawer/src/views/DrawerView.tsx
index a1c1ccf..2401c59 100644
--- a/node_modules/@react-navigation/drawer/src/views/DrawerView.tsx
+++ b/node_modules/@react-navigation/drawer/src/views/DrawerView.tsx
@@ -39,6 +39,7 @@ type Props = DrawerNavigationConfig & {
   state: DrawerNavigationState<ParamListBase>;
   navigation: DrawerNavigationHelpers;
   descriptors: DrawerDescriptorMap;
+  onStateChange: (state:any) => void
 };
 
 const getDefaultDrawerWidth = ({
@@ -88,6 +89,7 @@ export default function DrawerView({
   minSwipeDistance,
   sceneContainerStyle,
   detachInactiveScreens = true,
+  onStateChange = () => {}
 }: Props) {
   const [loaded, setLoaded] = React.useState([state.routes[state.index].key]);
   const dimensions = useWindowDimensions();
@@ -238,6 +240,7 @@ export default function DrawerView({
               swipeEnabled={swipeEnabled}
               onOpen={handleDrawerOpen}
               onClose={handleDrawerClose}
+              onStateChange={onStateChange}
               gestureHandlerProps={gestureHandlerProps}
               drawerType={drawerType}
               drawerPosition={drawerPosition}
